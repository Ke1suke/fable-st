# STATUS.md — セッション間の作業メモリ

(/handoff が更新し、新セッションには SessionStart フックが自動注入する。常に現在形・簡潔に保つ)

## 完了したこと
- Claude Code 高生産性セットアップ一式を構築(検証優先 + モデルルーティング + フック強制)
- 敵対的レビュー2回(adversary)+ 独立検証(verifier: v2 判定合格)+ 再構築2回を実施
- フック回帰テスト .claude/hooks/tests/test-hooks.sh: 153件全通過が最新の検証結果

## 現在の状態
- ブランチ: claude/repository-placement-n4cmlm
- このリポジトリはセットアップのテンプレート。実プロジェクトにはまだ適用していない
- CLAUDE.md の「プロジェクト固有情報」はプレースホルダのまま(適用先で書き換える前提)

## 次にやること
1. 実プロジェクトへ展開(README の導入手順に従う)
2. CLAUDE.md の「プロジェクト固有情報」を実際のビルド/テストコマンドに書き換える
3. verify.sh を使用言語に合わせて調整し、test-hooks.sh で回帰確認

## 未解決の問題・仮定
- フックは事故防止であり、セキュリティ境界ではない(README「既知の制約」参照)
- Stop フック(prompt型)の判定精度、/model opusplan の可用性は実運用で要確認
- サブエージェントの編集禁止はプロンプト規律(機械的強制は部分的)
