# プロジェクト運用ルール

## 大原則: 検証優先 (Verification-First)
- 実装タスクは必ず「検証手段の確認 → 実装 → 検証実行」の順で進める。
- 新機能・修正の前に、まず失敗するテストを書く(または既存テストの該当箇所を特定する)。
  テストが書けない場合は、何をもって完了とするかの検証手順をSTATUS.mdに明文化してから着手する。
- 「動くはず」で完了報告しない。lint/型チェック/テストの実行結果を根拠として示す。
- 検証に失敗したら、成功するまで修正ループを回す。3回失敗したら立ち止まり、
  アプローチ自体を疑って報告する(同じ修正の繰り返しは禁止)。

## モデルルーティング
- 探索・大量読み込み・ログ解析 → explorer サブエージェント(Haiku)に委譲する。
  メインコンテクストを検索結果やログで汚染しない。
- 設計判断・難しいデバッグ・アーキテクチャ決定 → architect サブエージェント(Opus)に委譲する。
- 実装の本体は現行モデル(Sonnet)で行う。
- 重要な計画・差分は adversary サブエージェントにレビューさせてから確定する。

## 状態管理 (STATUS.md プロトコル)
- 作業の節目(計画確定時、大きなタスク完了時、コンテクストが長くなった時)で STATUS.md を更新する。
- STATUS.md には「完了したこと / 現在の状態 / 次にやること / 未解決の問題・仮定」を書く。
- 新しいセッションやサブエージェントは、まず STATUS.md と CLAUDE.md を読んでから作業を開始する。
- 長時間タスクは1セッションで粘らず、検証が通った区切りで /handoff して新しいコンテクストに引き継ぐ。

## 計画と実行の分離
- 非自明なタスクは、いきなり実装せず計画を先に提示して承認を得る(plan modeを使う)。
- 計画には「変更するファイル / 検証方法 / リスクと仮定」を必ず含める。
- 計画の穴が疑われる場合は /redteam で外部モデル(GPT-5.5)によるレビュー用プロンプトを生成する。

## 禁止事項
- docker compose down / docker-compose down の実行(過去に環境破壊の事故あり)。
  必要な場合は理由を説明してユーザーの明示的な承認を得る。
- git push --force, git reset --hard, rm -rf の自発的実行。
- テストの削除・スキップによる「テストを通す」行為。
- 検証せずに「修正しました」と報告すること。

## プロジェクト固有情報(ここを各プロジェクトで書き換える)
- ビルド: `make build` (例。実際のコマンドに置換)
- テスト: `make test` (例)
- lint/型チェック: .claude/hooks/verify.sh が自動実行(中身をプロジェクトに合わせること)
- 主要ディレクトリ構成: (記載する)
