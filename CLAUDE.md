# プロジェクト運用ルール

## 大原則: 検証優先 (Verification-First)
- 実装タスクは必ず「検証手段の確認 → 実装 → 検証実行」の順で進める。
- 新機能・修正の前に、まず失敗するテストを書く(または既存テストの該当箇所を特定する)。
  テストが書けない場合は、何をもって完了とするかの検証手順を STATUS.md に明文化してから着手する。
- 「動くはず」で完了報告しない。lint/型チェック/テストの実行結果を根拠として示す。
  過去に通った記憶は証拠にならない。証拠は常に「いま実行した結果」。
- 一定規模以上のタスク(目安: 3ファイル超 or 100行超の変更)の完了報告前には
  verifier サブエージェントに独立検証させる。自己申告と独立検証は別物。
- 検証に失敗したら、成功するまで修正ループを回す。同じアプローチで3回失敗したら立ち止まり、
  アプローチ自体を疑って報告する(同じ修正の繰り返しは禁止)。バグ調査は /debug のプロトコルに従う。

## モデル戦略(Pro プラン・Fable なし運用)
- メインセッション: 日常の実装は Sonnet 5。設計・難デバッグ・大規模リファクタが主体の作業は Opus 4.8。
  「計画は深く、実行は速く」したい場合は /model opusplan(Plan Mode=Opus、実行=Sonnet)。
- サブエージェントへのルーティング:
  - 探索・大量読み込み・ログ解析 → explorer(Haiku)。メインの文脈を検索結果で汚染しない。
  - 設計判断・難しいバグの根本原因分析 → architect(Opus)。本当に難しい問題だけに使う。
  - 重要な計画・差分のレビュー → adversary。確定前に必ず通す。
  - 完了主張の独立検証 → verifier。
- 難問には拡張思考を使う(プロンプトに think hard / ultrathink)。定型作業には使わない。
- レート制限の節約: 同じ調査を二度しない(結果は STATUS.md へ)。大量のファイル読みをメインでしない。
  無関係な新タスクの前に /clear する。

## コンテクスト経済
- コンテクストは最重要リソース。長大なファイル・ログは直接読まず、explorer に要約させる。
- 1セッション1タスクを基本とし、無関係な作業を同一コンテクストに混ぜない。
- 会話が濁ってきたら(同じ誤りの繰り返し、過去の誤答への引きずられ)、粘らず /handoff → 新セッション。
- 自動 compact に任せるより、検証が通った区切りで /handoff する方が引き継ぎの質が高い。

## 状態管理 (STATUS.md プロトコル)
- 作業の節目(計画確定時、大きなタスク完了時、コンテクストが長くなった時)で STATUS.md を更新する。
- STATUS.md には「完了したこと / 現在の状態 / 次にやること / 未解決の問題・仮定」を書く。
- 新セッションには SessionStart フックが STATUS.md を自動注入する。肥大化させない(常に現在形に保つ)。
- STATUS.md と CLAUDE.md を書き換えるのはメインセッションだけ。サブエージェント・並列セッションには
  書かせない(後勝ち上書きの競合防止)。サブエージェントの報告はメインへの返答で受け取る。
- 長時間タスクは1セッションで粘らず、検証が通った区切りで /handoff して新しいコンテクストに引き継ぐ。

## 計画と実行の分離
- 非自明なタスクは、いきなり実装せず計画を先に提示して承認を得る(plan mode を使う)。
- 計画には「変更するファイル / 検証方法 / リスクと仮定」を必ず含める。
- 重要な計画は確定前に adversary にレビューさせる。外部視点が欲しい場合は /redteam で
  外部モデル(GPT-5.5)用レビュープロンプトを生成し、返答を /crossexam で消化する。
- 実装方針が複数あり不確実性が高い場合は /parallel で並列試作して検証結果で選ぶ。

## Git 規律
- main/master に直接コミットしない。作業はフィーチャーブランチで行う。
- 1論理変更 = 1コミット。動く状態でこまめに刻み、戻れる地点を作る。
- コミット前は /precommit を通す(検証再実行・秘密情報チェック・差分レビュー)。
- git push は明示的に依頼された場合のみ。force push はいかなる場合も自発的に行わない。

## セキュリティ
- .env・秘密鍵・認証情報は読まない・書かない・コミットしない(フックと permissions で強制済み)。
- 環境変数の「名前」だけ必要な場合は grep -oE '^[A-Z0-9_]+' .env のようにキー名のみ抽出する。
- コード・コミット・ログ出力に API キーやトークンを含めない。発見したら即座にユーザーへ報告する。
- 依存パッケージの新規追加時は、パッケージ名のタイポ(typosquatting)と実在・メンテ状況を確認する。

## 禁止事項
- docker compose down / docker-compose down の実行(過去に環境破壊の事故あり)。
  必要な場合は理由を説明してユーザーの明示的な承認を得る。
- git push --force, git reset --hard, git checkout/restore . での変更一括破棄, rm -rf の自発的実行。
- git commit --no-verify によるフック回避。フックが失敗するなら原因を直す。
- テストの削除・スキップ・アサーション弱体化による「テストを通す」行為。
- 検証せずに「修正しました」と報告すること。

## 学んだ教訓(/retro が追記する。10行を超えたら整理する)
- (まだなし)

## プロジェクト固有情報(ここを各プロジェクトで書き換える)
- ビルド: `make build` (例。実際のコマンドに置換)
- テスト: `make test` (例)
- lint/型チェック: .claude/hooks/verify.sh が自動実行(中身をプロジェクトに合わせること)
- 主要ディレクトリ構成: (記載する)
